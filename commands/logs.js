module.exports = {
  aliases: ['logs'],
  channel: ['text'],
  userPermissions: ['MANAGE_GUILD'],
  run(client, message, args) {
    return new Promise(async (resolve, reject) => {
      
    })
  }
}